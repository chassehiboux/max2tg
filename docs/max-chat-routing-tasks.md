# Задачи: маршрутизация MAX в Telegram-топики

## Статус

- Согласование требований: завершено
- Реализация: завершена
- Текущий фокус: финальная проверка и коммит

## Чек-лист

### 1. Обновить спецификацию и план

Статус: `done`

- зафиксировать модель одного Telegram-форума;
- зафиксировать автоматическое создание топиков;
- зафиксировать отправку сообщений владельца из топика обратно в MAX;
- зафиксировать reply в MAX через Telegram reply.

### 2. Расширить локальное хранилище

Статус: `done`

- добавить глобальные настройки рабочего форума;
- добавить поля топика в запись MAX-чата;
- добавить хранение соответствий Telegram-сообщений и MAX `message_id`;
- сделать мягкую миграцию старых записей без использования старых `tg_chat_id`.

### 3. Добавить выбор рабочего Telegram-форума

Статус: `done`

- добавить кнопку выбора форума в личке;
- использовать `KeyboardButtonRequestChat(chat_is_forum=True)`;
- сохранить выбранный форум;
- проверить, что форум доступен и бот может управлять топиками.

### 4. Создавать и обновлять топики

Статус: `done`

- автоматически создавать топик для каждого активного MAX-чата;
- сохранять `message_thread_id`;
- переименовывать топик при изменении названия MAX-чата;
- ставить сообщения в очередь при ошибке создания/доступа.

### 5. Отправлять MAX-сообщения в топики

Статус: `done`

- пробросить `message_thread_id` во все методы отправки Telegram;
- обновить delivery callback;
- сохранять связь отправленного Telegram-сообщения с MAX `message_id`;
- дозаливать очередь после создания топика.

### 6. Переделать ответы из Telegram в MAX

Статус: `done`

- отправлять любое текстовое сообщение владельца из топика в соответствующий MAX-чат;
- не удалять сообщение владельца из Telegram;
- не редактировать исходное сообщение из MAX после ответа;
- при Telegram reply отправлять в MAX `link={"type": "REPLY", "messageId": ...}`;
- сохранить кнопку `Ответить` как явный reply-сценарий.

### 7. Обновить управление чатами

Статус: `done`

- убрать выбор отдельной Telegram-группы для MAX-чата;
- показывать выбранный форум;
- показывать статус топика;
- оставить отключение и включение отслеживания.

### 8. Обновить тесты и документацию

Статус: `done`

- обновить тесты хранилища;
- обновить тесты маршрутизации;
- обновить тесты Telegram handler;
- обновить README и `.env.example`;
- выполнить точечные проверки перед коммитом.
